---
services:
  api:
    build: .
    ports:
      - "8080:8080"
    networks:
      - backend
      - extra

  extra-source:
    build:
      context: ./jobberwocky-extra-source
    ports:
      - "8090:8080"
    networks:
      - extra

networks:
  backend:
  extra:
